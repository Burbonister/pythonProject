# Write a Python program which accepts the radius of a circle from the user and compute the area.
import math
pi = math.pi

print(pi)


radius = float (input("insert radius of circle :\t"))
area = (radius**2)*pi
print ("area are :"+str(area))

print (radius)